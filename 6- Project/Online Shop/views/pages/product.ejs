<%- include('../header/header') %>
<%- include('../header/navbar') %>
    <div class="container">
        <div class="row mt-3">
            <div class="col col-12 col-md-4">
                <img src="/<%= ProdID.image %>" alt="<%= ProdID.image %>" width="200" height="200">
                <form action="/cart" method="POST" class="mt-2">
                    <input type="number" name="amount" class="form-control">
                    <input type="submit" value="Add To Cart" class="btn btn-outline-primary mt-2">
                </form>
            </div>
            <div class="col col-12 col-md-8">
                <h3><%= ProdID.name %></h3>
                <h5>Price: <%= ProdID.price %></h5>
                <p><%= ProdID.description %></p>
            </div>
        </div>
    </div>
<%- include('../footer/footer') %>