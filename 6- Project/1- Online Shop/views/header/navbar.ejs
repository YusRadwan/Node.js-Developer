<nav class="navbar navbar-expand-md bg-body-tertiary">
    <div class="container">

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">

        <!-- Pages -->
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link <%= titlePage === 'Home' ? 'active' : '' %>" aria-current="page" href="/">Online Shop</a>
            </li>
            <% if (isUser && !isAdmin) { %>
              <li class="nav-item">
                <a class="nav-link <%= titlePage === 'Cart' ? 'active' : '' %>" href="/cart">Cart</a>
              </li>
            <% } %>
            <% if (isAdmin) { %>
              <li class="nav-item">
                <a class="nav-link <%= titlePage === 'Add Product' ? 'active' : '' %>" href="/addproduct">Add Product</a>
              </li>
            <% } %>
          </ul>

        <!-- Registration -->
        <% if (!isUser && !isAdmin) { %>
          <ul class="navbar-nav mb-2 mb-lg-0 justify-content-end">
            <li class="nav-item  me-2">
              <a class="nav-text btn btn-primary <%= titlePage === 'Login' ? 'active' : '' %>" href="/login">login</a>
            </li>
            <li class="nav-item">
              <a class="nav-text btn btn-primary  <%= titlePage === 'Sign UP' ? 'active' : '' %>" href="/signup">signup</a>
            </li>
          </ul>
        <% } else { %>
          <ul class="navbar-nav mb-2 mb-lg-0 justify-content-end">
            <li class="nav-item  me-2">
              <a class="nav-text btn btn-danger" href="/logout">logout</a>
            </li>
          </ul>
        <% } %>



      </div>
    </div>
  </nav>